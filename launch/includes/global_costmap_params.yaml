global_costmap:

  global_frame: /map
  robot_base_frame: /base_link
  update_frequency: 5 # 2 The frequency in Hz for the map to be updated. (THIS CAUSES THE ERROR) 
  publish_frequency: 5 # 2 The frequency in Hz for the map to be publish display information. 
  static_map: true
  trasform_tolerance: 0.5 #added

  #rolling_window true
  #width 100.0
  #height 100.0
  #resolution 0.05
#  plugins:
  # - {name: static_layer, type: "costmap_2d::StaticLayer"}
  # - {name: obstacle_layer, type: "costmap_2d::ObstacleLayer"}
  # - {name: inflation_layer, type: "costmap_2d::InflationLayer"}
